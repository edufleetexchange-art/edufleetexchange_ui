# User Decomposition — Design Spec

The canonical spec lives in the server repo:

`edufleetexchange/docs/superpowers/specs/2026-05-24-user-decomposition-design.md`

This refactor touches both repos (server is the bulk; UI updates types, `AuthContext`, and consumer pages — see §7 of the canonical spec). Keep edits in the canonical copy to avoid drift.
